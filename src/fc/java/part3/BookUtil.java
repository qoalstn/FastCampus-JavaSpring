package fc.java.part3;

public class BookUtil {
    public void bookInfoPrintByUtil(BookDTO book){
        System.out.println("BY URIL >> " + "title = " + book.title + "price = " + book.price + "company = " + book.company);
    }
}
